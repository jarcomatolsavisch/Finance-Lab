services:
  api:
    build: ./api/tw-stocks
    ports:
      - "8088:8088"
    environment:
      - FINMIND_APIKEY=${FINMIND_APIKEY}

  frontend:
    build: ./frontend
    ports:
      - "8089:8089"
    environment:
      - API_BASE_URL=http://api:8088
    depends_on:
      - api
